"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { toast } from "sonner";
const roles = ["SUPERADMIN", "ADMIN", "CASHIER", "USER"];
export default function StaffDirectory({ users }: { users: { id: string; name: string; role: string; stores: string[] }[] }) { const router = useRouter(); const [busy, setBusy] = useState<string | null>(null); const [error, setError] = useState(""); const [inviteError, setInviteError] = useState(""); async function update(id: string, role: string) { setBusy(id); setError(""); const response = await fetch(`/api/admin/staff/${id}/role`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ role }) }); if (!response.ok) setError((await response.json()).error ?? "Role update failed."); else { toast.success("Staff role updated"); router.refresh(); } setBusy(null); } async function invite(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setInviteError(""); const form = new FormData(event.currentTarget); const response = await fetch("/api/admin/staff/invitations", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: form.get("email"), role: form.get("role") }) }); const data = await response.json(); if (!response.ok) return setInviteError(data.error ?? "Invitation could not be sent."); event.currentTarget.reset(); toast.success("Staff invitation sent"); } return <section className="mx-auto max-w-6xl"><p className="text-sm font-semibold text-amber-700">Superadmin only</p><h1 className="mt-1 text-3xl font-bold text-amber-950">Staff access</h1><p className="mt-2 text-slate-600">Cashiers require a store assignment before they can process orders.</p><form onSubmit={invite} className="mt-6 grid gap-3 rounded-2xl border bg-white p-5 sm:grid-cols-3"><label className="grid gap-1 text-sm font-semibold">Staff email<input required name="email" type="email" className="min-h-11 rounded-lg border px-3" /></label><label className="grid gap-1 text-sm font-semibold">Role<select name="role" className="min-h-11 rounded-lg border px-3"><option value="CASHIER">Cashier</option><option value="ADMIN">Admin</option></select></label><button className="min-h-11 self-end rounded-lg bg-amber-700 px-4 font-bold text-white">Send invitation</button>{inviteError && <p role="alert" className="text-sm text-red-700 sm:col-span-3">{inviteError}</p>}</form>{error && <p className="mt-4 text-red-700" role="alert">{error}</p>}<div className="mt-6 divide-y rounded-2xl border bg-white">{users.map((user) => <article key={user.id} className="flex flex-wrap items-center justify-between gap-4 p-4"><div><h2 className="font-semibold text-amber-950">{user.name}</h2><p className="text-sm text-slate-600">Stores: {user.stores.join(", ") || "No store assignment"}</p></div><label className="sr-only" htmlFor={`role-${user.id}`}>Role for {user.name}</label><select id={`role-${user.id}`} defaultValue={user.role} disabled={busy === user.id} onChange={(event) => update(user.id, event.target.value)} className="min-h-11 rounded-lg border px-3"><option value={user.role}>{user.role}</option>{roles.filter((role) => role !== user.role).map((role) => <option key={role}>{role}</option>)}</select></article>)}</div></section>; }
