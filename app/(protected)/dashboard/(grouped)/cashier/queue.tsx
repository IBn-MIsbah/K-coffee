"use client";
import { Button } from "@/components/ui/button";
import { useRouter } from "next/navigation";
import { useState } from "react";
const next: Record<string, string | undefined> = { PENDING: "CONFIRMED", CONFIRMED: "PREPARING", PREPARING: "READY_FOR_PICKUP", READY_FOR_PICKUP: "COMPLETED" };
export default function CashierQueue({ orders }: { orders: { id: string; orderNumber: string; status: string; itemCount: number; pickupTime: string | null; timezone: string }[] }) { const router = useRouter(); const [busy, setBusy] = useState<string | null>(null); const [error, setError] = useState(""); async function advance(id: string, status: string) { setBusy(id); setError(""); const response = await fetch(`/api/staff/orders/${id}/status`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ nextStatus: status }) }); if (!response.ok) setError((await response.json()).error ?? "Update failed."); else router.refresh(); setBusy(null); } return <section className="mx-auto max-w-6xl"><p className="text-sm font-semibold text-amber-700">Staff workspace</p><h1 className="mt-1 text-3xl font-bold text-amber-950">Pickup queue</h1><p className="mt-2 text-slate-600">Only orders assigned to your store are shown.</p>{error && <p role="alert" className="mt-4 text-red-700">{error}</p>}<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{orders.map((order) => <article key={order.id} className="rounded-2xl border bg-white p-5 shadow-sm"><p className="font-mono text-sm font-bold">{order.orderNumber}</p><p className="mt-2 font-semibold">{order.status.replaceAll("_", " ")}</p><p className="mt-2 text-sm text-slate-600">{order.itemCount} items · {order.pickupTime ? new Intl.DateTimeFormat("en-ET", { timeZone: order.timezone, timeStyle: "short" }).format(new Date(order.pickupTime)) : "No pickup time"}</p>{next[order.status] && <Button className="mt-5 min-h-11 w-full" disabled={busy === order.id} onClick={() => advance(order.id, next[order.status]!)}>{busy === order.id ? "Updating…" : `Mark ${next[order.status]!.replaceAll("_", " ")}`}</Button>}</article>)}</div>{!orders.length && <div className="mt-6 rounded-2xl border border-dashed p-8 text-center text-slate-600">No active pickup orders are assigned to this store.</div>}</section>; }
