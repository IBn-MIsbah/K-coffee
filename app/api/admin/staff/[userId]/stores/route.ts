import { AuthenticationError, AuthorizationError, requireRole } from "@/lib/authz";
import prisma from "@/lib/prisma";
import { UserRole } from "@/lib/rbac";
export async function PUT(request: Request, { params }: { params: Promise<{ userId: string }> }) { try { const actor = await requireRole([UserRole.SUPERADMIN]); const { userId } = await params; const { storeIds } = await request.json(); if (!Array.isArray(storeIds) || storeIds.some((id) => typeof id !== "string")) return Response.json({ error: "storeIds must be an array." }, { status: 422 }); const result = await prisma.$transaction(async (tx) => { const stores = await tx.storeLocation.findMany({ where: { id: { in: storeIds }, isActive: true }, select: { id: true } }); if (stores.length !== new Set(storeIds).size) throw new Error("INVALID_STORE"); const user = await tx.user.findUnique({ where: { id: userId }, select: { id: true } }); if (!user) throw new Error("NOT_FOUND"); await tx.staffStoreAssignment.deleteMany({ where: { userId } }); if (storeIds.length) await tx.staffStoreAssignment.createMany({ data: [...new Set(storeIds)].map((storeId) => ({ userId, storeId })) }); await tx.auditLog.create({ data: { userId: actor.id, userRole: actor.role, action: "store_assignment", resource: "users", resourceId: userId, details: { storeIds } } }); return { storeIds }; }); return Response.json(result); } catch (error) { if (error instanceof AuthenticationError) return Response.json({ error: error.message }, { status: 401 }); if (error instanceof AuthorizationError) return Response.json({ error: error.message }, { status: 403 }); if (error instanceof Error && error.message === "INVALID_STORE") return Response.json({ error: "One or more stores are unavailable." }, { status: 422 }); if (error instanceof Error && error.message === "NOT_FOUND") return Response.json({ error: "User not found." }, { status: 404 }); return Response.json({ error: "Unable to save assignments." }, { status: 500 }); } }
